# E2E-test med Playwright

Det här projektet innehåller end-to-end-tester för webbsidan:

https://lejonmanen.github.io/agile-helper/

Tester är skrivna i Python med Playwright och Pytest.

## Installation

Installera beroenden:

```bash
pip install pytest-playwright
playwright install